using System;
namespace DateTime;
class Program
{
    public static void Main(string[] args)
    {
        /*
        System.DateTime date = new System.DateTime(2021, 8, 10, 10, 40, 32);
        Console.WriteLine($"{date.Year}\n{date.Month}\n{date.Day}\n{date.Hour}\n{date.Minute}\n{date.Second}");
        string dateTime = date.ToString("yyyy,MM,dd,hh,mm,ss,tt");
        string[] dateTimeArray = dateTime.Split('/', ':', ' ');
        Console.Write("Output : ");
        for (int i = dateTimeArray.Length - 1; i >= 0; i--)
        {
            Console.Write($"{dateTimeArray[i]} ");
        }
        Console.WriteLine(date)


        Console.WriteLine($"\nEnter details");

        bool temp = true;
        System.DateTime date1;

        do
        {
            temp = System.DateTime.TryParseExact(Console.ReadLine(), "yyyy/MM/dd hh:mm:ss tt", null, System.Globalization.DateTimeStyles.None, out date1);

        } while (!temp);
        Console.WriteLine(date1.ToString("yyyy, MM, dd"));

        */




    }




}
}